sorting:
  model_id: google/gemma-3-270m
  save_dir: outputs/reordered_llms/reordered_new_null_magnitude_magnitude
  dtype: auto
  device: auto
  device_map: auto
  calibration_file: data/calibration/calibration.txt
  chunk_len: 2048

  # null leaves that component in its original order.
  heads_metric: null
  neurons_metric: magnitude
  embeddings_metric: magnitude
  top_k: 3
  postcheck: true

# Run separately with: python plot_importance.py -c <this-file>
plot_importance:
  # Required. Use the timestamped model directory produced by sort_llm.py.
  # model_path: outputs/reordered_llms/reordered_new_null_magnitude_magnitude_YYYYMMDD_HHMMSS/model
  out_dir: outputs/importance_analysis/null_magnitude_magnitude
  calibration_file: data/calibration/calibration.txt
  chunk_len: 2048
  device: auto
  dtype: auto
  heads_metric: null
  neurons_metric: magnitude
  embeddings_metric: magnitude
